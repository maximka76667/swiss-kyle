# Wiki Log

Append-only. Claude writes one entry per operation. Do not edit manually.

---

[2026-06-28] CREATE wiki/issues/user-friendly-process-errors → created: user-friendly-process-errors.md | updated: index.md
[2026-06-28] UPDATE wiki sweep — stale content removed: system-overview, frontend, worker, cli-publisher, http-api, index
[2026-08-07] CREATE wiki/issues/shallow-vs-deep-module-audit ← src-tauri/src/{video_server,job_log,commands,lib}.rs, crates/worker/src/{cut_video,merge_pdfs,convert_document}.rs, crates/shared/src/lib.rs, ui/src/hooks/use-file-drop.ts, ui/src/components/{cut-video,doc-converter,merge-pdfs,job-history,video-player}.tsx (Ousterhout-style deep/shallow-module and information-hiding read-through requested by the user, who is reading A Philosophy of Software Design; identified Registry, JobLog, the worker run() functions, and useFileDrop as deep modules, and flagged four open shallow/leaky spots — duplicated file-pick/validate/submit logic across the three tool components, DocFormat/Converter knowledge duplicated between crates/shared and doc-converter.tsx, repeated TrackedJobStatus narrowing across four functions in job-history.tsx, and get_pdf_page_count as a depth outlier among commands.rs's otherwise-thin pass-through commands. Filed as open — no refactoring applied yet, per the user's instruction to review file-by-file themselves) → created: wiki/issues/shallow-vs-deep-module-audit | updated: wiki/index.md
[2026-08-07] UPDATE src-tauri/src/pdfcpu.rs (new), src-tauri/src/{commands,lib}.rs, crates/worker/src/merge_pdfs.rs, e2e/support/ffmpeg.ts (new), e2e/specs/{cut-video,merge-pdfs}.spec.ts, e2e/fixtures/{sample-a.pdf,sample-b.pdf,phone-sparse-keyframes.mp4} (new) → acted on the shallow-vs-deep-module-audit finding by extracting get_pdf_page_count's pdfcpu subprocess+JSON-parsing logic out of commands.rs into a new pdfcpu.rs module (page_count(bin, path)), matching the existing video_server.rs/job_log.rs pattern; separately fixed a real bug surfaced while testing it — pdfcpu merge refuses to overwrite an existing output file by default, failing a same-title re-merge — by adding --force, chosen over a UI confirmation for consistency with cut_video.rs's existing ffmpeg -y overwrite behavior; added two e2e regression tests: merge-pdfs.spec.ts merges two PDFs then resubmits with the same title but reversed order (via the row list's own reorder control) and asserts the output bytes actually changed, since a "Done" status alone can't distinguish a real overwrite from a no-op; cut-video.spec.ts adds a user-supplied real phone-recorded clip (renamed sample-a.pdf/sample-b.pdf/phone-sparse-keyframes.mp4 for clarity — originally recycled dev-output filenames) that historically surfaced a separate, earlier keyframe-spacing bug in cut_video.rs (fixed in 8b35908, predating this session) and verifies via a new countDecodedVideoFrames helper (e2e/support/ffmpeg.ts, locates the bundled ffmpeg sidecar by directory scan rather than a hardcoded target triple) that the cut output actually contains decoded video frames, not just that the job reported Done — closing the same class of gap (status alone can't prove correctness) in both new tests. Also corrected a pre-existing, unrelated staleness found in the process: wiki/components/worker.md's cut_video.rs section still documented the old `-c copy` stream-copy command, contradicting the actual re-encode fix already live in source since 8b35908 → created: wiki/issues/pdfcpu-merge-refuses-overwrite | updated: wiki/components/tauri-app (get_pdf_page_count → pdfcpu.rs delegation), wiki/components/worker (merge_pdfs.rs section added; cut_video.rs command and Decisions & Rationale corrected to match actual re-encode behavior; Summary/Sources/Related updated), wiki/components/e2e-tests (specs table, fixtures list, new ffmpeg.ts support helper, Sources/Related), wiki/index.md
[2026-08-07] UPDATE wiki/components/worker.md (follow-up sweep of the same stale `-c copy` claim after the user asked for a recap of the prior fix — a second, separate sentence in the Decisions & Rationale ack-timing paragraph still read "ffmpeg cuts are `-c copy` (seconds)", missed in the first pass because that edit targeted the cut_video.rs section specifically and didn't scan the rest of the file for the same claim; also the bottom `## Related` list still lacked the e2e-tests/pdfcpu-merge-refuses-overwrite links already added to the frontmatter `Related` field in the prior edit) → updated: wiki/components/worker

[2026-08-19] RENAME/UPDATE Cut Video → Edit Video, adding a crop feature end-to-end (`feature/video-cropping` branch): `crates/shared/src/lib.rs` (`CutVideo`→`EditVideo`, new `CropRect { x, y, width, height }: u32` native-pixel-unit struct), `crates/worker/src/cut_video.rs`→`edit_video.rs` (conditional `-vf crop=w:h:x:y`, width/height rounded to even for libx264), `crates/worker/src/{main,job}.rs`, `crates/worker/src/bin/publisher.rs`, `src-tauri/src/{commands,lib}.rs` (`submit_cut_job`→`submit_edit_video_job`), `ui/src/components/cut-video.tsx`→`edit-video.tsx`, new `ui/src/components/crop-overlay.tsx` (always-visible corner-bracket drag handles, no dark mask — tried one, user asked to remove it after seeing it dim the native video controls), new `ui/src/lib/tools.ts` (single source of truth for the sidebar/routes/job-history-icon list, replacing three separate copies in tool-nav.tsx/App.tsx/job-history.tsx), `ui/src/types/jobs.ts` (`CropRect` is a *class*, not a plain type — its constructor rounds every field, since the drag math is float but the value crosses IPC into a Rust `u32` field; a plain `roundRect()` helper was tried first and rejected by the user for being forgettable at new call sites). Four real bugs found via the user's own manual testing (not by e2e) and fixed: (1) unrounded crop floats hitting the Rust u32 field, caught by the literal `invoke()` error message; (2) the crop overlay's container lacked `pointer-events-none`, silently blocking every click on the native video player controls, not just the crop area; (3) trim/crop/submit controls had no disabled-until-metadata-loaded gating at all, so using them before the video's duration/dimensions loaded silently produced broken near-zero-duration jobs; (4) `VideoPlayer` was keyed on `inputPath`, so re-picking the *same* file left it unchanged, meaning React never remounted it and its internal metadata state never reloaded — the parent's own reset state (`metadataLoaded`) then had nothing to repopulate it, permanently disabling everything. Also added `ui/src/lib/utils.ts`'s `clamp`/`letterbox` with `ui/src/lib/utils.test.ts` — first frontend unit tests in the repo, via `bun:test` (needed `"bun"` added to `tsconfig.app.json`'s `types` array); wired into `bun run test:frontend` (root `package.json`, and `ui/package.json`'s own `test` script) and a new CI `frontend` job step, kept deliberately separate from `test:unit` (`cargo test`) rather than merged, since merging would either duplicate frontend runs across `rust-unit-tests`' OS matrix or force that job to stop being matrixed. E2E: `e2e/specs/cut-video.spec.ts`→`edit-video.spec.ts`, plus `navigation.spec.ts`; added tests for the crop handle changing the on-screen indicator, a crop job producing an actually-smaller output, submitting untouched, and loading several videos in a row (the last two are regression tests for bugs 3 and 4 above, at the user's explicit request to write tests before fixing). The crop-drag e2e assertions themselves went through several dead ends before landing: WebdriverIO's Actions API move with `origin: 'pointer'` + relative deltas is a known, unfixed upstream bug (webdriverio/webdriverio#4134) — fixed by using absolute viewport coordinates for every move step; the crop handle can exist in the DOM before `crop-overlay.tsx`'s own `ResizeObserver` layout measurement has run, so `dragCropHandle` polls for a real non-zero size first; and `e2e/support/ffmpeg.ts`'s `getVideoDimensions` regex matched ffmpeg's hex codec tag (`0x31637661`) before the real resolution, extracting `width=0` and making every crop assertion fail with a nonsensical `< 0` threshold — this was mistaken for a feature bug for a long time before being traced to the regex itself. `e2e/wdio.conf.ts`'s `logLevel` dropped from `info` to `warn` (the verbose per-command logging was only ever needed for the CI investigation, not routine runs). README.md and this wiki sweep both updated in the same pass → created: none | updated: wiki/components/job-types (EditVideo, CropRect), wiki/components/worker (edit_video.rs section, `-vf crop`), wiki/components/frontend (EditVideo throughout, new Crop and Frontend-unit-tests subsections, `tools.ts` single-source-of-truth note), wiki/components/e2e-tests (specs table, new getVideoDimensions/dragCropHandle subsections), wiki/architecture/system-overview (EditVideo throughout), wiki/components/tauri-app (submit_edit_video_job), wiki/issues/{e2e-file-drop-listener-race,e2e-linux-native-click-unreliable,pdfcpu-merge-refuses-overwrite,shallow-vs-deep-module-audit,user-friendly-process-errors} (Sources wikilinks repointed to renamed files — historical prose left as-is), wiki/index.md, README.md

<!-- Format:
[YYYY-MM-DD] OPERATION source/path → created: [pages] | updated: [pages]
-->

[2026-06-22] BOOTSTRAP docs/DESIGN.md, src/lib.rs, src/bin/subscriber.rs, src/bin/publisher.rs, src/bin/api.rs → created: wiki/architecture/system-overview, wiki/components/job-types, wiki/components/publisher, wiki/components/worker, wiki/components/cli-publisher, wiki/components/http-api, wiki/decisions/adr-001-local-only, wiki/decisions/adr-002-keep-nats-for-durability, wiki/decisions/adr-003-embedded-surrealdb, wiki/concepts/jetstream-pull-consumer, wiki/dependencies/async-nats, wiki/dependencies/axum, wiki/issues/missing-db-and-progress, wiki/issues/api-rs-obsolescence | updated: wiki/index.md

[2026-06-28] UPDATE crates/shared/src/lib.rs, crates/worker/src/main.rs, src-tauri/src/lib.rs, src-tauri/src/video_server.rs, swiss-kyle-ui/ (3 commits: JobEnvelope+id, StatusEvents, VideoPlayer) → created: wiki/components/tauri-app, wiki/components/video-server, wiki/components/frontend | updated: wiki/components/job-types (added JobEnvelope/JobStatus/StatusEvent), wiki/components/worker (progress parsing, StatusEvent publishing, graceful error handling), wiki/issues/missing-db-and-progress (progress now done; DB still missing), wiki/issues/api-rs-obsolescence (resolved), wiki/index.md

[2026-07-02] UPDATE crates/worker/src/{main,consumer,job}.rs, crates/shared/src/lib.rs, src-tauri/src/{lib,video_server}.rs, swiss-kyle-ui/src/components/video-player.tsx (hardening: ack heartbeats, consumer config reconciliation, worker split into consumer/job modules; video-server token registry replacing ?path=) → updated: wiki/components/worker (module split, ack_wait/heartbeat/max_deliver, ensure_consumer, spawn_blocking; corrected convert_to_pdf → convert_document), wiki/components/video-server (token registry, get_stream_url, resolve_range, DNS-rebinding rationale), wiki/concepts/jetstream-pull-consumer (persisted-config reconciliation, redelivery via heartbeats; stale "failures terminal" note revised), wiki/index.md

[2026-07-02] UPDATE prepare-sidecars.ts, src-tauri/tauri.conf.json, src-tauri/binaries/ → updated: wiki/components/tauri-app (sidecar binaries: prepare-sidecars.ts auto-download, cross-platform triples, externalBin; removed false "Linux x86_64 only" claim), wiki/architecture/system-overview (prepare-sidecars.ts; Linux/macOS/Windows support). Note: stale scripts/prepare-sidecars.sh (drifted to typst 0.13.0 vs .ts 0.15.0, unreferenced) deleted by user.

[2026-07-03] UPDATE prepare-sidecars.ts, src-tauri/src/lib.rs, src-tauri/tauri.conf.json, src-tauri/Cargo.toml (three production bugs found and fixed while testing a real packaged Linux build for the first time: pkill -f worker killing VS Code on dev runs; externalBin's next-to-exe placement causing packaged builds to hang on startup and .deb installs to conflict with system ffmpeg/nats-server packages; fatal()'s dialog hanging silently on Linux due to GTK event-loop timing) → created: wiki/decisions/adr-004-private-sidecar-resources, wiki/issues/prepare-sidecars-pkill-broad-match, wiki/issues/sidecar-path-resolution-usr-bin-collision, wiki/issues/fatal-dialog-hang-linux | updated: wiki/components/tauri-app (startup sequence, sidecar binaries section rewritten for resources/mainBinaryName, commands table +2, shutdown pidfile cleanup, known issues), wiki/architecture/system-overview (bundled binaries line: +pdfcpu, resources not externalBin), wiki/index.md

[2026-07-02] UPDATE wiki sweep — reconciled pre-existing drift against current source (verified against crates/shared/src/lib.rs, src-tauri/src/lib.rs, swiss-kyle-ui/src/{App,main,components/doc-converter,components/tool-nav,types/jobs}) → updated: wiki/components/job-types (added ConvertDocument/DocFormat/Converter), wiki/architecture/system-overview (ConvertToPdf→ConvertDocument, convert-document path, diagram), wiki/components/tauri-app (4 commands incl. submit_doc_convert_job/get_stream_url, VideoServer state), wiki/components/frontend (word-to-pdf→doc-converter throughout, DocConverter dropdowns, pendingEvents buffering, get_stream_url), wiki/issues/user-friendly-process-errors (source link), wiki/issues/onlyoffice-x2t-broken (resolved: x2t removed from code+UI, confirmed via repo grep), wiki/index.md

[2026-07-07] RENAME swiss-kyle-ui/ → ui/ (folder + package.json name + index.html title; tauri.conf.json frontendDist/beforeDevCommand/beforeBuildCommand; README repo layout) → updated: wiki/components/frontend, wiki/components/video-server (source paths swiss-kyle-ui/ → ui/)

[2026-07-07] CREATE e2e/ (WebdriverIO + tauri-driver suite: smoke/sidecars/cut-video/doc-converter/merge-pdfs specs, support/{selectors,drag-drop,navigate}.ts; src-tauri/capabilities/default.json +core:window:allow-close for IPC-driven window close in tests; wdio.conf.ts global `after` hook closing the window per spec, fixing a sidecar-process leak across spec files) → created: wiki/components/e2e-tests, wiki/issues/e2e-sidecar-leak-across-specs | updated: wiki/components/frontend (added Merge PDFs tool throughout — layout diagram, tool descriptions, Tool type, output paths; corrected stale claim that drag-and-drop skipped extension validation, now disproven by e2e coverage), wiki/index.md

[2026-07-07] UPDATE crates/shared/src/lib.rs, .env.development (new), .gitignore, e2e/package.json, e2e/specs/cut-video.spec.ts (added a real happy-path job-submission test; base_output_dir() now redirects to `.development/` in any debug build via checked-in .env.development, sandboxed to a base-name-only value so it can't escape the gitignored folder; e2e's `test` script now rebuilds worker then app.exe before running, since rebuilding the worker alone doesn't refresh the resource copy the running app spawns from) → created: wiki/issues/tauri-resource-copy-only-on-app-rebuild | updated: wiki/components/e2e-tests (output redirection section, self-sufficient test script, cut-video happy-path spec, sources), wiki/index.md, README.md

[2026-07-09] UPDATE decision (conversation, not a src/ file): full SurrealDB job-status/history persistence dropped — no functional need identified, disposable/in-memory history accepted as intentional design; SurrealDB kept but rescoped to a write-only per-job diagnostic log (including intermediate/detail entries, not just done/failed), to be surfaced via a new in-app Logs page → updated: wiki/decisions/adr-003-embedded-surrealdb (rewritten for new scope), wiki/issues/missing-db-and-progress (closed as won't-do, successor noted), wiki/architecture/system-overview (diagram + status bullets), wiki/components/worker, wiki/components/tauri-app, wiki/components/frontend (stale "no SurrealDB writes"/"no persistence layer" tech-debt framing corrected to reflect the intentional decision), wiki/index.md

[2026-07-08] UPDATE package.json (new root scripts test:unit/test:e2e), README.md, e2e/wdio.conf.ts (root-caused and fixed intermittent full-suite e2e failures: a fresh WebView2 session launched too soon after the previous one closed crashed the new app.exe within ~1.5s, found via process-tree polling showing orphaned msedgewebview2.exe children of an already-dead app.exe PID; a condition-based wait — polling for the app process's own absence — was tried first and performed worse, since that process disappears fast regardless of the real still-settling resource; settled on an empirically binary-searched fixed delay, 4000ms, verified 0/20 failures vs ~4% still failing at 3500ms) → created: wiki/issues/webview2-session-crash-on-fast-relaunch (replaces the earlier, less precise wiki/issues/webview2-session-churn-flakiness, deleted) | updated: wiki/components/e2e-tests (root cause + tuning table, root-level test:e2e script), wiki/index.md

[2026-07-10] UPDATE e2e/support/{click,navigate,drag-drop}.ts (new click.ts), e2e/specs/{cut-video,sidecars}.spec.ts, e2e/specs/navigation.spec.ts (new), e2e/repeat.ts (new), e2e/tsconfig.json, ui/src/hooks/use-file-drop.ts, ui/src/components/{cut-video,doc-converter,merge-pdfs}.tsx, crates/worker/Cargo.toml, prepare-sidecars.ts, src-tauri/{tauri.conf.json,capabilities/default.json,src/lib.rs} (root-caused and fixed the first real Linux run of the e2e suite failing across the board — three unrelated bugs: (1) native WebDriver pointer clicks don't reliably activate elements via wry's Linux backend, confirmed by controlled comparison producing zero successful navigations vs. reliable success dispatching `.click()` on the DOM element directly via `browser.execute()` — DPI scaling and window-decoration-offset theories both investigated and ruled out by direct experiment first; (2) useFileDrop's onDragDropEvent() listener registration is an async IPC round-trip that can lose a drop fired before it resolves, exposed as `ready`/`data-drop-ready` instead of inferring readiness from rendered text; (3) sidecars.spec.ts's close test failing for two layered reasons — an expected Linux-only "session died before it could reply" error from the close call itself (not a crash, confirmed via empty coredumpctl/dmesg and clean-ordered process-snapshot tracing; a relaunch-timing-crash theory was tried first, built into an 8-iteration stress-loop version of the test, then disproven by a keep-alive-ping experiment and reverted), and separately `isProcessRunning("worker")` always returning true because it matched Linux kernel `kworker/*` threads, fixed by renaming the worker crate/binary to `swiss-kyle-worker`. Verified across all three fixes by 30 consecutive full-suite runs, 180/180 spec files passed, zero failures) → created: wiki/issues/e2e-linux-native-click-unreliable, wiki/issues/e2e-file-drop-listener-race, wiki/issues/e2e-sidecars-linux-close-and-worker-match | updated: wiki/components/e2e-tests (support helpers, specs table +navigation.spec.ts, new repeat.ts section, known issues — removed the now-false "Linux untested" claim), wiki/components/frontend (useFileDrop ready/data-drop-ready), wiki/components/tauri-app (worker → swiss-kyle-worker throughout), wiki/components/worker (package rename note), wiki/decisions/adr-004-private-sidecar-resources (related link), wiki/issues/webview2-session-crash-on-fast-relaunch (noted the Linux failure was initially, incorrectly, suspected to be this same bug — confirmed unrelated), wiki/issues/prepare-sidecars-pkill-broad-match (related link — second, independent instance of the same substring-collision class of bug), wiki/index.md

[2026-08-19] UPDATE .github/workflows/ci.yml (root-caused the CI e2e job — added in 6708211 and never yet green — failing 100% of specs on both matrix legs with a `POST /session` timeout to tauri-driver, distinct from the known intermittent webview2-session-crash-on-fast-relaunch issue since even the very first session of a run failed. Two unrelated causes: on Linux, tauri-driver is spawned once in wdio.conf.ts's onPrepare hook, in the WDIO launcher process — outside the per-worker xvfb-run wrapping @wdio/xvfb provides automatically, so WebKitGTK had no display to initialize against; fixed by wrapping the whole Linux CI step in `xvfb-run`, per Tauri's own official CI recipe, rather than relying on @wdio/xvfb's per-worker mechanism. Confirmed fixed by the next Linux run passing cleanly. On Windows, the failure signature is different (`session not created: DevToolsActivePort file doesn't exist` from msedgedriver itself, after ~60s) — windows-latest preinstalls an EdgeDriver already on PATH, but version-matched to the standalone Edge browser app, not the separately-versioned WebView2 Runtime the app actually embeds; added a step installing msedgedriver-tool (reads the real WebView2 Runtime version from the registry, downloads a matching driver) ahead of it on PATH. First attempt at this fix reproduced the identical Windows failure unchanged; root-caused to the fix's own `run:` step using the default pwsh shell, which doesn't fail a step on a native .exe's nonzero exit — a silent `cargo install`/download failure there would fall through to the same mismatched preinstalled driver with no visible error. Hardened with explicit $LASTEXITCODE checks and a Test-Path/`--version` print; not yet re-verified) → created: wiki/issues/e2e-ci-session-creation-timeout | updated: wiki/components/e2e-tests (Sources/Related), wiki/index.md

[2026-08-19] UPDATE .github/workflows/ci.yml (dropped Windows from the e2e job after three separate fixes for the session-creation timeout — msedgedriver version match, ruling out an app-side setup() hang via the app's own log file, WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--disable-gpu against the GPU-hang theory from tauri-apps/tauri#2530 — each failed to change the identical failure at all; rust-unit-tests keeps Windows since it's plain cargo test, no WebView2. Also fixed an unrelated real bug hit along the way: scripts/sidecar-downloads.ts's unauthenticated api.github.com call for BtbN/FFmpeg-Builds release metadata hit GitHub's 60/hour unauthenticated rate limit, shared across all GitHub-hosted runners worldwide, causing intermittent 403s unrelated to this workflow; now sends Authorization: Bearer $GITHUB_TOKEN when set, wired via ci.yml job-level env on rust-unit-tests and e2e; also renamed the rust-tests job id to rust-unit-tests and changed the workflow trigger from push to pull_request) → updated: wiki/issues/e2e-ci-session-creation-timeout (Windows closed as won't-fix), wiki/index.md

[2026-07-12] UPDATE prepare-sidecars.ts, mark-sidecar-version.ts, fix-appimage-typst.ts, sidecar-versions.ts (root-caused a release bug: `md → pdf` conversion failed in packaged Linux/macOS builds with pandoc's own "Unknown output format typst" error. Cause: `release.yml`'s Linux job apt-installs `pandoc` — Ubuntu 22.04's apt repo is frozen at 2.9.2.1, predating pandoc's typst writer (added 3.1.2) — then `mark-sidecar-version.ts` blindly wrote the pinned-version marker file regardless of what was actually installed, so `prepare-sidecars.ts` trusted the stale apt binary and never redownloaded the real pin. Fixed by making `mark-sidecar-version.ts` verify the installed binary's own `--version` output against the pin before trusting it, and download the real pinned build itself (reusing prepare-sidecars.ts's own download machinery) on any mismatch instead of failing or trusting blindly — self-healing regardless of platform or which package manager (apt/brew) supplied it. ffmpeg is exempted from verification: its pin is a GitHub release tag never echoed in `ffmpeg -version` output. Refactored the shared download logic (`extract`/`downloadBinary`/per-tool URL maps) out of prepare-sidecars.ts into a new sidecar-downloads.ts so both scripts use one source of truth. Also moved all five sidecar scripts from the repo root into a new scripts/ directory) → created: scripts/sidecar-downloads.ts | updated: wiki/architecture/system-overview, wiki/components/e2e-tests, wiki/components/tauri-app, wiki/issues/e2e-sidecars-linux-close-and-worker-match, wiki/issues/prepare-sidecars-pkill-broad-match, wiki/issues/tauri-resource-copy-only-on-app-rebuild (path references updated to scripts/), README.md
